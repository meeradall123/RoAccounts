// Runtime config for hosted checkout integrations
// Set this to your Stripe Payment Link URL (e.g., https://buy.stripe.com/xxx)
// Payouts to your IBAN/BIC are configured within your Stripe (or PSP) dashboard.
window.CONFIG = {
  stripePaymentLink: "", // TODO: replace with your live Payment Link
};